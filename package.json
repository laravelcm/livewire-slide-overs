{
  "scripts": {
    "dev": "npx @tailwindcss/cli -i resources/css/slide-over.css -o public/slide-over.css --watch",
    "build": "npx @tailwindcss/cli -i resources/css/slide-over.css -o public/slide-over.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4.2.1"
  }
}
